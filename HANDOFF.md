Last updated: 2026-07-19 22:05 EDT

## ⏭ Next session: where to stand and what to do

Stand at software `19a41c25897d83835ed549b797fd41a23046414b`, the complete
bounded S2 slow-reference trajectory authority. S2 is provenance-closed through
Article implementation receipt `b92c616d672fb63a54040d693c94797ab9c08543`,
authored wiki roadmap `746037be982e22b065db7b38a1417fb6acc8bd21`, generated
wiki dashboard `0b80bf7f89ee2a46c61cba3b51ebbd616ee29182`, and final Article
closure `e84754e7945ca5cb86b0973100180da90e7dba7e`.

The next program gate is S3: formulate and ratify a separate fixed contract for
an optimized mixed-tetromino engine and its differential equivalence to the S2
oracle. Do not implement S3, run Easley, or acquire data until that contract is
approved.

## Pre-flight checks

- Preserve foreign untracked `.omx/` and `.pi-subagents/`; never stage them.
- Confirm `main`, `origin/main`, and the expected S2 authority agree and inspect
  `git status --short` before any edit.
- Run the shared-repository bi-directional preflight before coordinated Article
  or wiki work.
- Re-read the Article `INDUSTRIALIZATION-REPORT.md`,
  `S2-REMAINING-CLOSURE-CONTRACT.md`, and current wiki roadmap before drafting
  S3; the Article owns program decisions.
- Treat `tetris_ballistic.engine.reference_trajectory` as an immutable oracle
  surface unless a separately reviewed compatibility repair is required.

## Concrete deliverables for the next session

1. Draft a bounded S3 contract specifying optimized API boundaries, exact
   reference/optimized comparison observables, widths/horizons/roots,
   performance and memory thresholds, package/ABI coverage, failure rules, and
   explicit exclusions.
2. Obtain PI sign-off on every engineering or resource choice before code.
3. Decide whether to run a local-only Graphify pilot over the public simulation
   repository to inspect module coupling and likely S3 blast radius. Evaluate
   it in an isolated clone first; do not commit generated graph artifacts or
   install hooks until usefulness and maintenance cost are reviewed.
4. After ratification only, implement S3 as its own bounded unit and preserve
   the S2 reference trajectory as the differential oracle.

## Success criteria for the immediate next step

- The S3 contract is explicit, finite, adversarially reviewable, and ratified.
- Every S3 equivalence observable and performance/resource ceiling has an exact
  testable definition.
- The contract opens no S4a, S1b, C0, E1, release, or scientific claim.
- Any Graphify pilot remains reproducible, local, isolated, and demonstrably
  useful for a concrete architecture question before repository adoption.

## Anti-success criteria

- No optimized-engine implementation before contract ratification.
- No Easley/Slurm submission, acquisition, treatment inspection, or E1 work.
- No root API, CLI, legacy, scheduler, or production routing inferred from S2.
- No committed generated architecture graph merely because a tool can produce
  one; avoid stale derived artifacts and overlapping git-hook ownership.
- No modification or staging of `.omx/` or `.pi-subagents/`.

## What the previous sessions produced

- Software `19a41c25897d83835ed549b797fd41a23046414b` adds the explicit-only
  typed bounded reference trajectory, fixed 512-checkpoint schedule, canonical
  exact-integer JSON/checksum identity, complete checkpoint/resume, independent
  lower-layer reconstruction, and a sole validated terminal manifest.
- Section 6 passes 320 exhaustive small-state configurations, 256 randomized
  adversarial prefixes, terminal 769 with exactly 512 checkpoints and resume
  from 256, the fixed 16,384-event exit sweep, all 19 orientations under both
  contact laws, four slow certificates, and the final default suite
  `1953 passed, 6 skipped, 23 deselected`.
- Ruff/format/compile/diff, hostile review, clean wheel/sdist/Twine/member
  audits, isolated Python 3.13 wheel smoke, and fresh-origin readback pass.
- Wheel SHA-256 is
  `0c61077dc28866e4c10337acba69cd06bc57e2bdb2b0db91e960a2d6bd6b4951`;
  sdist SHA-256 is
  `3eb89ffe2e74d028ba0afc4a42ecb4499a6a0c7381a1cdd2402e5b5417b8f05c`.
- The Article and wiki reciprocal provenance loop is closed at the hashes in
  the first section; the public roadmap now explicitly records S3 through E1
  and the pre-E1 Easley roles.

## Repo & git state at handoff

- Repository: `/home/lechen/Dropbox/Public/Simulations_on_Some_Surface_Growth_Models`
- Branch: `main`
- S2 software authority: `19a41c25897d83835ed549b797fd41a23046414b`
- Expected tracked state after this dedicated handoff commit: clean.
- Expected untracked user-owned state: `.omx/`, `.pi-subagents/` only.
- Remote: public GitHub origin; `origin/main` must match the handoff-bearing
  commit before work resumes.

## Operating notes

- The reference trajectory is deliberately absent from package-root exports
  and has no I/O, CLI, legacy, optimized, scheduler, or production route.
- Independent reconstruction intentionally bypasses the writer's high-level
  selection/placement trajectory methods and replays through certified lower
  primitives.
- The fixed checkpoint schedule contains 512 strictly increasing ordinals,
  terminal minimum 769, and digest
  `47b85984ab7903f48a0e06437c3f3c8a297b82db683287bc1e515d624933aeaa`.
- If assessing Graphify, prefer AST-only local extraction, a narrow
  `.graphifyignore`, disabled query logging, and an isolated clone. Do not let
  its Codex installer rewrite the repository AGENTS.md or install overlapping
  hooks during the pilot.

## Risks to monitor

- An optimized implementation could accidentally share high-level code with
  the oracle and weaken differential independence.
- Generated graph/report artifacts can grow quickly and become stale; measure
  graph size, rebuild time, diff churn, and whether queries find real boundary
  violations before adopting them.
- Graph tools distinguish extracted and inferred edges, but inferred edges are
  not software authority and must never replace source/test evidence.
- Easley availability may tempt premature performance or scientific work; only
  separately authorized S3 certification, then later C0e/C0a/C0b stages, may
  use it before E1.
